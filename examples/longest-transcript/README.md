# Longest-transcript example

```bash
biohub fasta longest-transcript -f input.fa -o longest.fa
```

Expected records: `txA.2` and `txB.1`. This fixture is synthetic. Publication
claims require separate deidentified real-data fixtures and domain-approved golden
outputs.
